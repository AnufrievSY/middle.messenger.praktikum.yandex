# Middle Messenger

Учебный проект мессенджера для Яндекс Практикума. В этом этапе добавлены TypeScript, компонентный подход с Block и EventBus, клиентская генерация страниц и формы с валидацией.

## Страницы проекта

- `#/login` — страница авторизации
- `#/register` — страница регистрации
- `#/settings` — страница профиля
- `#/chats` — список чатов и лента переписки
- `#/404` — страница ошибки 404
- `#/500` — страница ошибки 500

## Установка и запуск

Установить зависимости:
```bash
npm install
```

Запуск проекта в режиме разработки:
```bash
npm run dev
```

Сборка проекта:
```bash
npm run build
```

Проверка типов и линтинга:
```bash
npm run lint
npm run lint:eslint
npm run lint:styles
npm run typecheck
```

Запущенно на [Netlify](https://taupe-beijinho-bc1893.netlify.app/)

## Тестовый пользователь

Можно войти под демо-пользователем:
- login: `demo`
- password: `Demo1234`

Если при регистрации введены уже существующие `login` или `email`, автоматически используется тестовый пользователь.
